providers = ["php"]

[phases.setup]
  nixPkgs = ["php84", "php84Packages.composer", "nodejs_22"]

[phases.build]
  cmds = [
    "composer install --no-dev --optimize-autoloader",
    "npm ci",
    "npm run build"
  ]
